the `mcp-completions` proxy adds authenticated mcp to any chat-completions endpoint.

TODO:

- Add oauth for context as well so people are requested to login into parallel for https://llmtext.com links and uithub links.
- Make it a responses endpoint as well
- Determine where the boundary is and draw a graph of what this actually adds
